{
  "name": "bluesky-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "A personal, single-user MCP server for Bluesky / AT Protocol, deployable to Netlify.",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "tsc --noEmit",
    "deploy": "netlify deploy --build --prod"
  },
  "dependencies": {
    "@atproto/api": "^0.13.35"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "@types/node": "^22.10.0",
    "netlify-cli": "^17.38.0",
    "typescript": "^5.7.2"
  }
}
